<component name="VcsDirectoryMappings">


component name="VcsDirectoryMappings">ript${
    scrip${selectedText}
    search and heal
    {viruses${cwd}
    buggs${cwd}
    ransomware claims undo${cwd}
    spyware${cwd}
    adware${cwd}
    breaches in matrix
    {rss}

    